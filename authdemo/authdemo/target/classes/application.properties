# Nome da aplica��o Spring Boot
# Este identificador � usado em logs, atua��o (actuator) e descoberta de servi�os
spring.application.name=authdemo

# ============================================================
# CONFIGURA��ES DO BANCO DE DADOS (PostgreSQL)
# ============================================================

# URL de conex�o com o banco de dados PostgreSQL
# Formato: jdbc:postgresql://host:porta/nome_do_banco
# localhost: o banco est� rodando na mesma m�quina que a aplica��o
# 5432: porta padr�o do PostgreSQL
# db_suaoficina: nome do banco de dados que deve existir previamente no PostgreSQL
spring.datasource.url=jdbc:postgresql://localhost:5432/db_suaoficina

# Nome de usu�rio para autentica��o no PostgreSQL
# Deve ter permiss�es de leitura, escrita e cria��o de tabelas (para ddl-auto=update)
# Substitua pelo seu usu�rio real do banco de dados (ex: postgres, admin, etc.)
spring.datasource.username=postgres

# Senha correspondente ao usu�rio do banco de dados
# ATEN��O: Em produ��o, nunca armazene senhas em texto puro no arquivo de propriedades.
# Utilize vari�veis de ambiente ou servi�os de segredos (ex: Spring Cloud Vault, AWS Secrets Manager)
spring.datasource.password=p6$Tmina

# ============================================================
# CONFIGURA��ES DE E-MAIL (SMTP do Gmail)
# ============================================================
# Estas configura��es s�o usadas pelo JavaMailSender para enviar e-mails
# de recupera��o de senha. O servi�o UserService utiliza este componente.

# Servidor SMTP do Gmail - respons�vel por enviar os e-mails
spring.mail.host=smtp.gmail.com

# Porta do servidor SMTP com TLS (Transport Layer Security)
# 587 � a porta padr�o para STARTTLS (conex�o normal que sobe para TLS)
# Alternativa seria 465 para SSL direto, mas 587 � a recomendada atualmente
spring.mail.port=587

# E-mail remetente que ir� enviar os tokens de recupera��o
# Deve ser um endere�o Gmail v�lido (ex: suaoficina.app@gmail.com)
spring.mail.username=levasi.backup@gmail.com

# Senha de aplicativo (App Password) gerada no Google Account
# ATEN��O: N�o � a senha normal do Gmail. Para contas com verifica��o em duas etapas,
# � necess�rio gerar uma "Senha de Aplicativo" espec�fica para este servi�o.
# Acesse: Conta Google > Seguran�a > Senhas de aplicativo
spring.mail.password=ovub rzqn ojzm xbyy

# Habilita autentica��o no servidor SMTP
# true: o servidor exigir� usu�rio e senha v�lidos
spring.mail.properties.mail.smtp.auth=true

# Habilita TLS (STARTTLS) - converte a conex�o SMTP comum em uma conex�o criptografada
# true: solicita ao servidor que utilize TLS ap�s o handshake inicial
# Sem esta op��o, o e-mail seria enviado em texto puro (inseguro)
spring.mail.properties.mail.smtp.starttls.enable=true

# ============================================================
# CONFIGURA��ES DO JPA (Java Persistence API) E HIBERNATE
# ============================================================

# Estrat�gia de gera��o autom�tica do esquema do banco de dados
# update: atualiza o esquema existente ou cria tabelas novas sem destruir dados.
# Outras op��es poss�veis:
#   - create: apaga todas as tabelas e recria do zero (perde dados)
#   - create-drop: igual ao create, mas apaga as tabelas ao encerrar a aplica��o
#   - validate: apenas verifica se o esquema est� compat�vel com as entidades
#   - none: n�o faz nenhuma altera��o autom�tica
# ATEN��O: update em produ��o pode ser arriscado. O ideal � usar ferramentas de migra��o
# como Flyway ou Liquibase em ambientes produtivos.
spring.jpa.hibernate.ddl-auto=update

# Exibe as consultas SQL geradas pelo Hibernate no console
# �til para desenvolvimento e depura��o. Em produ��o, deve ser false ou configurado
# para logging espec�fico, pois exibir cada consulta pode sobrecarregar os logs.
spring.jpa.show-sql=true

# Define o dialeto SQL que o Hibernate deve usar para gerar consultas otimizadas
# para o PostgreSQL. O dialeto informa ao Hibernate quais tipos de dados, fun��es
# e sintaxe espec�fica usar para o banco de dados em quest�o.
# Sem esta configura��o, o Hibernate pode gerar SQL incompat�vel com o PostgreSQL.
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

// TLS/HTTPS
server.port=8443
server.ssl.key-store=classpath:keystore.p12
server.ssl.key-store-password=senha123
server.ssl.key-store-type=PKCS12
server.ssl.key-alias=authdemo

Chave AE
encryption.secret=ChaveSecreta

//Arquivo de log
logging.file.name=logs/auditoria.log
logging.level.com.pfc.suaoficina.authdemo.service.UserService=INFO